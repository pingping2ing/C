(7주차)

69. 차,전화 번호
	x != y 조건 지우고 실행
	2018 중간고사 II. 3  답구하기

4자리 숫자로 0이 들어있지 않고 모두 다른 숫자
첫째 숫자가 마지막 숫자보다 2큼
둘째 숫자가 셋째 숫자보다 큼
집 전화번호 뒤의 4자리를 역순으로 배열하면 번호판의 수와 같음

번호판 숫자와 집 전화번호 뒤의 4자리를 합하면 16456


2018 중간고사 II. 3
[이상해 교수의 차번호] 건망증이 심한 이상해 교수는 차번호에 대해 수첩에 다음과 같은 메모를 기록했다.
① 4자리의 숫자로 모두 다른 숫자임 
② 첫번째 숫자가 네번째 숫자보다 큼 // 두번째 숫자가 제일 작지는 않다. (문제 오류, 없어야함)
③ 첫번째 숫자와 두번째 숫자의 차이는 6이다.
④ 집 전화번호 뒤의 4자리를 역순으로 배열하면 번호판의 수와 같음
조교가 전화번호를 질문하니, “차번호와 집 전화번호 뒤의 4자리를 합하면 내 강좌번호인 15675가 된다네...”
차번호는 얼마인가?

73. 문자 회전 암호화  <= 생략

81. 초 : 시 분 초
	

82. 화씨 <-> 섭씨

84. 서로 소인가? (공약수가 1뿐임)
	2018 중간고사 IV. (9)  답구하기

* 친화수 추가
2018 기말고사 7 (1)

* 소수 추가 (쌍둥이 소수)
2018 기말고사 8 (1)

#include <stdio.h>

int main() {
	/*int car_no, home_no;
	int a, b, c, d;
	for (a = 3; a < 10; a++) {
		for (b = 2; b < 10; b++) {
			for (c = 1; c < b; c++) {
				d = a - 2;
				car_no = 1000 * a + 100 * b + 10 * c + d;
				home_no = 1000 * d + 100 * c + 10 * b + a;
				if (car_no + home_no == 16456 && a != b && a != c) printf("%d", car_no);
			}
		}
	}*/ //연습문제 69
	/*int sec, min=0, hour=0,second=0;
	printf("계산할 초를 입력하시오: ");
	scanf("%d", &sec);
	second = sec;
	for (; sec >= 60; sec -= 60) min++;
	for (; min >= 60; min -= 60) hour++;
	printf("%d초: %02d시:%02d분:%02d초", second, hour, min, sec);
	*/ //연습문제 81

	/*double f, c=0;
	for (f = -10; f <= 130; f += 5) {
		c = (double)5 / 9 * (f - 32);
		printf("화씨 %.f= 섭씨 %.1f\n", f, c);
	}
	*/ //연습문제 82

	/*int a, b, flag = 0;
	scanf("%d%d", &a, &b);

	for (int i = 2; i < (a > b ? a : b) / 2; i++) {
		if (a % i == 0 && b % i == 0) {
			flag++; break;
		}
	}
	if (flag) printf("서로소 입니다");
	else printf("서로소가 아닙니다");
	*/ //연습문제 84



}