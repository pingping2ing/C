p.178	예제 5-3-1  2중루프 (디지털근)
		7849 테스트	(오류 수정 >=)


	예제 5-3-4	abc + cac = bdd
		d가 짝수라는 사실을 이용해서 코드 수정하도록 할 것
		a+c < 10 이라는 사실을 이용해서 코드 수정하도록 할 것

예제 5-7-4
	(1) d가 짝수라는 사실을 이용해서 코드 수정하도록 할 것
	(2) a+c 가 10보다 작다는 사실을 이용해서 코드 수정하도록 할 것


예제 5-8-1	난수
	line 7, 8 주석 처리 후 실행
	line 8 을 srand(0); 으로 하고 실행


예제 5-8-4
	코드 단순화하는 방법 생각하기 ( 비긴경우, LOSS/WIN 먼저 설정하고 특정 조건에서 값 바꿔주기 )

5장. 연습문제

11. 갤런 -> 리터
	2017 중간고사 I. 1  답구하기

13. 유클리드 호제법
	2018 중간고사 IV. 9  문제 읽어보기

17. acb - bbc = bdc    => acb = bdc + bbc 
	2b는 a보다 작거나 같다 => a는 9가 최대 => b는 4이하 0은 아님. (* a,b,c가 다르다는 조건이 코드에 없음)
	b - c = c  또는 10+b - c = c  (b는 짝수)

18. aa + bb + cc = cba
	c = 1 또는 2
	a+b+c = 10 + a	=> b = 9 또는 8


2017 1.1
1. A씨는 작년 여름 미국에 여행을 다녀왔다. 렌트카를 이용해 Death Valley를 관광하였는데, 경치는 너무 훌륭했지만 기온이 화씨 124도여서 너무 더웠었다. 렌트카는 15 갤런의 기름을 사용하였고, 414 마일을 운행하였다. (문제) 화씨 124도는 섭씨 몇도인가? 그리고 렌트카의 연비(km/liter)는 얼마인가? (둘 다 소수 첫째자리까지만 구함-반올림. 섭씨=(5/9)*(화씨-32), 1갤런은 3.7854 리터, 1마일은 1.6093 km로 가정.)

2018 4.9

9. 1030502030 부터 1030503030까지 수 중에서 서로 소(素)가 되는 쌍(A, B)은 몇 개인가? (A,B)와 (B,A)는 같은 것으로 한다. 그리고 서로 소가 되는 쌍 중에서 쌍을 이루는 두 수의 차(B-A)가 가장 큰 것을 (A,B) 형식으로 표시하여라.

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#define WIN 1
#define LOSS 0

int main() {
	/*int number, sum;
	printf("숫자를 입력하시오: ");
	scanf("%d", &number);
	while (number >= 10) {
		sum = 0;
		while (number != 0) {
			sum += number % 10;
			number /= 10;
		}
		number = sum;
		printf("==> %d", number);
	}*/ //예제 5-3-1  2중루프 (디지털근)
	/*int a = 1, b, c, d, abc, cac, bdd;
	while (a < 10) {
		b = 1;
		while (b < 10) {
			c = 1;
			while (c < 10) {
				d = 0;
				while (d < 10) {
					abc = 100 * a + 10 * b + c;
					cac = 100 * c + 10 * a + c;
					bdd = 100 * b + 10 * d + d;
					if (abc + cac == bdd) printf(" %d\n+%d\n-----\n %d\n\n",abc,cac,bdd);
					d+=2;
				}
				c++;
			}
			b++;
		}
		a++;
	}*/ //예제 5-3-4
	/*unsigned int i, seed;
	//seed = time(0);
	srand(0);
	for (i = 0; i < 10; i++) printf("%d ", rand());
	*/ //예제 5-8-1

	/*int i, num, in;
	srand(time(NULL));
	for (i = 0; i < 3; i++) {
		num = rand() % 3;
		printf("!!!자 시작합니다!!!\n");
		printf("가위! 바위! 보! (가위=0,바위=1,보=2): ");
		scanf("%d", &in);
		if (in < 0 || in>2) { 
			printf("잘못 입력하였습니다.\n"); 
			continue;
		}
		if (in == num) printf("비겼습니다~\n");
		else if (in == 0 && num == 2 || in > num) printf("당신이 이겼습니다!!\n");
		else printf("컴퓨터가 이겼네요.\n");
		printf("컴퓨터: %d  YOU: %d\n\n", num, in);
	}*/ //예제 5-8-4

	/*double F_tem=124, C_tem, l,km,cost;
	C_tem = (F_tem - 32)*5 / 9;
	l = 15 * 3.7854;
	km = 414 * 1.6093;
	cost = km / l;
	printf("섭씨= %.1f 렌트카의 연비=%.1f", C_tem, cost);
	*/ //11번

	/*int x, y,z;
	scanf("%d%d", &x, &y);
	while (y != 0) {
		z = x%y;
		x = y;
		y = z;
	}
	printf("최대공약수= %d", x);
	*/ //13번

	/*int a=1, b, c, d, acb, bbc, bdc;
	while (a < 10) {
		b = 1;
		while (b < 5) {
			c = 0;
			while (c < 10) {
				d = 0;
				while (d < 10) {
					acb = 100 * a + 10 * c + b;
					bbc = 100 * b + 10 * b + c;
					bdc = 100 * b + 10 * d + c;
					if (acb - bbc == bdc) printf(" %d\n-%d\n-----\n %d\n\n", acb, bbc, bdc);
					d++;
				}
				c++;
			}
			b++;
		}
		a++;
	}*/ // 17번

	/*int aa, bb, cc, cba,a=1,b,c;
	while (a < 10) {
		b = 1;
		while (b < 10) {
			c = 1;
			while (c < 3) {
				aa = 10 * a + a;
				bb = 10 * b + b;
				cc = 10 * c + c;
				cba = 100 * c + 10 * b + a;
				if (aa + bb + cc == cba) printf(" %d\n %d\n+%d\n-----\n%d", aa, bb, cc, cba);
				c++;
			}
			b++;
		}
		a++;
	}*/ // 18번
}